import styled from "@emotion/styled";

import appTheme from "../../styles/appTheme";

const StyledH2 = styled.h2`
  color: ${appTheme.colors.tertiary.lighter};
  font-weight: bolder;
  font-size: ${appTheme.fontSize.sm};
  line-height: 1;
  text-align: center;
`;

export default StyledH2;
